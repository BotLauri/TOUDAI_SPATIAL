# Spatial project about the movement of people in Sweden 
# based on the learning institutions in that particular area.
setwd("~/GitHub/TOUDAI_SPATIAL")
data <- read.csv(file = "nice_data.csv")
head(data)


